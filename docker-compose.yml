version: '3.8'

services:
  backend:
    build: 
      context: ./be
      dockerfile: Dockerfile
    ports:
      - "5000:5000"
    environment:
      - DB_HOST=boovkd434xr0zfz4t0mu-mysql.services.clever-cloud.com
      - DB_USER=ufiog8sb8qoqwfor
      - DB_PASSWORD=cVuFz7kXWKbG1LTkf7EK
      - DB_NAME=boovkd434xr0zfz4t0mu
    networks:
      - employee-app-network

  frontend:
    build: 
      context: ./fe
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    depends_on:
      - backend
    networks:
      - employee-app-network


networks:
  employee-app-network: